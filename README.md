# SiPy-Web

Open Source programming language.


example codes:
a = integer(10)
b = integer(20)
c = a.add(b)

loop(1, "writelm(c)")


a = integer(10)
b = integer(20)

ifcase(a.eq(b), "writeln('a equals b')", "writeln('a does not equal b')")
